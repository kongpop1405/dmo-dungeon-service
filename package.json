{
  "name": "dmo-dungeon-service",
  "version": "1.0.0",
  "description": "DMO Dungeon Service — FB poster generator (HTML -> PNG)",
  "type": "module",
  "scripts": {
    "serve": "python -m http.server 8777",
    "export": "node build/export_poster.mjs"
  },
  "devDependencies": {
    "playwright": "^1.61.0"
  }
}
